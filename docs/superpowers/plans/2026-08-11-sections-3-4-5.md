# Sections 3, 4, 5 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build area setup, the home map with Mapbox GL JS, and the segment sheet — the core app flow from sign-in to picking a segment to run.

**Architecture:** Separate HTML pages per section, each with inline JS (no build step). Share code via lib/ modules for auth, API calls, constants, and utilities. Mapbox GL JS will be the center of the map screen with segments stored in Supabase and displayed as pins. Segment sheet is a draggable bottom sheet over the map.

**Tech Stack:**
- Mapbox GL JS 3.x (pinch-zoom, pan, clustering)
- Supabase JS client 2.x (auth already set up, use for CRUD)
- Overpass API (harvest segments into segments table)
- Mapbox Geocoding API (reverse geocode for area names)

## Global Constraints

- Mapbox GL JS, not Static Images. The map must pinch-zoom and pan.
- Mapbox token in code (lib/constants.js), never ask user to paste.
- Segments harvested from Overpass into database and kept. The map reads from the table.
- Every pin shows a badge with how many people ran that segment today, from runs table. No badge when zero.
- No stubs, no placeholder data, no fake counts. If not built, throw or show real error.
- Every page gets `<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">`.
- Every network call gets timeout (5s default) and visible error state.
- Test on deployed Vercel URL, never localhost.
- All code is inline HTML + JS, no external build. CDN imports (Supabase, Mapbox) via `<script>` tags.

---

## File Structure

**New files:**
- `section-3-area-setup.html` — Area setup screen with Mapbox GL map, draggable pin, reverse geocode, search
- `section-4-home-map.html` — Home map screen, Mapbox GL JS, pins with badges, card strip, tabs
- `section-5-segment-sheet.html` — Segment sheet, bottom draggable sheet, board, elevation profile
- `lib/constants.js` — Supabase URL/key, Mapbox token, timeouts, API endpoints
- `lib/api.js` — Supabase CRUD (profiles, segments, runs), Overpass harvest, Mapbox geocoding
- `lib/harvest.js` — OpenStreetMap Overpass query builder and segment processing
- `lib/utils.js` — Geometry helpers (haversine, polyline encode/decode, bbox bounds), formatting

**Modified files:**
- `lib/auth.js` — Already working, no changes needed

---

## Task 1: Create lib/constants.js

**Files:**
- Create: `lib/constants.js`

**Interfaces:**
- Produces: Global constants for all sections
  - `SUPABASE_URL` (string)
  - `SUPABASE_ANON_KEY` (string)
  - `MAPBOX_TOKEN` (string)
  - `TIMEOUTS` (object) — `{ api: 5000, geocode: 5000, overpass: 30000, location: 10000 }`
  - `OVERPASS_API` (string)
  - `MAPBOX_GEOCODING_API` (string)

**Steps:**

- [ ] **Step 1: Write lib/constants.js with all required values**

The Mapbox token must be a public, URL-restricted token. Create it in the Mapbox dashboard:
1. Go to https://account.mapbox.com/tokens/
2. Click "Create a token"
3. Name it "Trailblazer"
4. Ensure "Public Scopes" includes `maps:read`, `styles:read`, `tokens:read`
5. Restrict URL to `https://trailblazer-khn2.vercel.app` (and `https://localhost:*` for testing)
6. Copy the token

```javascript
/**
 * Trailblazer: Constants
 * Supabase, Mapbox, and API configuration
 */

const SUPABASE_URL = 'https://ttrhmwjnegnbnovnwlzo.supabase.co';
const SUPABASE_ANON_KEY = 'sb_publishable_wJyTy7emvg4M4EKIkd320Q_uwZFZ6xM';
const MAPBOX_TOKEN = 'pk.eyJ1IjoiZ2VvcmdlYWxsbmVyIiwiYSI6ImNsbzg4eHRkcTBka3kybnJueWNrNWU2ODYifQ.XXXXXXXXXXXXXXXXXXXX'; // Substitute real token

const TIMEOUTS = {
  api: 5000,        // 5s for Supabase CRUD
  geocode: 5000,    // 5s for Mapbox geocoding
  overpass: 30000,  // 30s for Overpass (can be slow)
  location: 10000   // 10s for geolocation
};

const OVERPASS_API = 'https://overpass-api.de/api/interpreter';
const MAPBOX_GEOCODING_API = 'https://api.mapbox.com/geocoding/v5/mapbox.places';

// Export for use in modules
window.AppConstants = {
  SUPABASE_URL,
  SUPABASE_ANON_KEY,
  MAPBOX_TOKEN,
  TIMEOUTS,
  OVERPASS_API,
  MAPBOX_GEOCODING_API
};
```

- [ ] **Step 2: Commit**

```bash
git add lib/constants.js
git commit -m "feat: add app constants (Supabase, Mapbox, API endpoints)"
```

---

## Task 2: Create lib/utils.js — Geometry and Formatting Helpers

**Files:**
- Create: `lib/utils.js`

**Interfaces:**
- Consumes: Nothing
- Produces:
  - `haversine(lat1, lng1, lat2, lng2)` → distance in metres
  - `degreesToRadians(degrees)` → radians
  - `formatDistance(metres)` → "1.2 km" or "450 m"
  - `formatTime(seconds)` → "4:32" or "1h 23m"
  - `formatPace(seconds, metres)` → "8:30/km"
  - `polylineEncode(points)` → encoded string
  - `polylineDecode(encoded)` → [[lat, lng], ...]
  - `isPointInBounds(lat, lng, bounds)` → boolean
  - `pointToPixel(lat, lng, map)` → {x, y} on canvas
  - `getInitials(name)` → "GA"

**Steps:**

- [ ] **Step 1: Write haversine and geometry helpers**

```javascript
/**
 * Trailblazer: Utility functions
 * Geometry, formatting, conversions
 */

function degreesToRadians(degrees) {
  return degrees * (Math.PI / 180);
}

function haversine(lat1, lng1, lat2, lng2) {
  const R = 6371000; // Earth's radius in metres
  const φ1 = degreesToRadians(lat1);
  const φ2 = degreesToRadians(lat2);
  const Δφ = degreesToRadians(lat2 - lat1);
  const Δλ = degreesToRadians(lng2 - lng1);
  
  const a = Math.sin(Δφ / 2) * Math.sin(Δφ / 2) +
            Math.cos(φ1) * Math.cos(φ2) *
            Math.sin(Δλ / 2) * Math.sin(Δλ / 2);
  const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
  return R * c;
}

function formatDistance(metres) {
  if (metres < 1000) {
    return Math.round(metres) + ' m';
  }
  return (metres / 1000).toFixed(1) + ' km';
}

function formatTime(seconds) {
  const mins = Math.floor(seconds / 60);
  const secs = Math.round(seconds % 60);
  if (mins < 60) {
    return `${mins}:${String(secs).padStart(2, '0')}`;
  }
  const hours = Math.floor(mins / 60);
  const minsRem = mins % 60;
  return `${hours}h ${minsRem}m`;
}

function formatPace(seconds, metres) {
  // Pace in min:sec per km
  const paceSeconds = (seconds / metres) * 1000;
  return formatTime(paceSeconds) + '/km';
}

function polylineEncode(points) {
  // Google's polyline encoding algorithm (https://developers.google.com/maps/documentation/utilities/polylinealgorithm)
  let encoded = '';
  let prevLat = 0, prevLng = 0;
  
  for (const [lat, lng] of points) {
    const dlat = Math.round((lat - prevLat) * 1e5);
    const dlng = Math.round((lng - prevLng) * 1e5);
    
    [dlat, dlng].forEach(val => {
      let v = val << 1;
      v = v < 0 ? ~v : v;
      while (v >= 0x20) {
        encoded += String.fromCharCode((0x20 | (v & 0x1f)) + 63);
        v >>= 5;
      }
      encoded += String.fromCharCode(v + 63);
    });
    
    prevLat = lat;
    prevLng = lng;
  }
  return encoded;
}

function polylineDecode(encoded) {
  // Decode Google polyline
  let points = [];
  let index = 0, lat = 0, lng = 0;
  
  while (index < encoded.length) {
    let result = 0, shift = 0, byte;
    do {
      byte = encoded.charCodeAt(index++) - 63;
      result |= (byte & 0x1f) << shift;
      shift += 5;
    } while (byte >= 0x20);
    
    const dlat = (result & 1) ? ~(result >> 1) : (result >> 1);
    lat += dlat;
    
    result = 0;
    shift = 0;
    do {
      byte = encoded.charCodeAt(index++) - 63;
      result |= (byte & 0x1f) << shift;
      shift += 5;
    } while (byte >= 0x20);
    
    const dlng = (result & 1) ? ~(result >> 1) : (result >> 1);
    lng += dlng;
    
    points.push([lat / 1e5, lng / 1e5]);
  }
  return points;
}

function isPointInBounds(lat, lng, bounds) {
  // bounds = { minLat, minLng, maxLat, maxLng }
  return lat >= bounds.minLat && lat <= bounds.maxLat &&
         lng >= bounds.minLng && lng <= bounds.maxLng;
}

function getInitials(name) {
  return name
    .split(' ')
    .map(w => w[0])
    .join('')
    .toUpperCase()
    .slice(0, 2);
}

// Export to window
window.AppUtils = {
  degreesToRadians,
  haversine,
  formatDistance,
  formatTime,
  formatPace,
  polylineEncode,
  polylineDecode,
  isPointInBounds,
  getInitials
};
```

- [ ] **Step 2: Test haversine with known values**

According to CLAUDE.md, haversine should return:
- 69.2 m for 0.001° longitude at 51.5°N
- 111.2 m for 0.001° latitude

Open browser console on deployed page and run:
```javascript
AppUtils.haversine(51.5, 0, 51.5, 0.001)  // Should be ~69.2
AppUtils.haversine(51.5, 0, 51.501, 0)    // Should be ~111.2
```

- [ ] **Step 3: Test formatting functions**

```javascript
AppUtils.formatDistance(450)     // "450 m"
AppUtils.formatDistance(1200)    // "1.2 km"
AppUtils.formatTime(272)         // "4:32"
AppUtils.formatTime(3600)        // "1h 0m"
AppUtils.getInitials('George Allner')  // "GA"
```

- [ ] **Step 4: Test polyline encode/decode**

```javascript
const pts = [[38.5,-120.2],[40.7,-120.95],[43.252,-126.453]];
const enc = AppUtils.polylineEncode(pts);
console.log(enc);  // Should be: _p~iF~ps|U_ulLnnqC_mqNvxq`@
const dec = AppUtils.polylineDecode(enc);
console.log(dec);  // Should match original pts (within rounding)
```

- [ ] **Step 5: Commit**

```bash
git add lib/utils.js
git commit -m "feat: add geometry and formatting utilities"
```

---

## Task 3: Create lib/api.js — Supabase CRUD and External APIs

**Files:**
- Create: `lib/api.js`

**Interfaces:**
- Consumes:
  - `AppConstants` (from lib/constants.js)
  - `AppUtils` (from lib/utils.js)
  - `window.supabaseClient` (from lib/auth.js)
- Produces:
  - `getProfile(userId)` → {id, display_name, level, home_lat, home_lng, home_label} or error
  - `updateProfile(userId, data)` → {error: null | object}
  - `getSegmentsInBounds(bounds, limit=200)` → [{id, name, highway, length_m, geom, start_lat, start_lng, ...}] or error
  - `getRunsForSegmentToday(segmentId)` → [{id, user_id, seconds, level}] or error
  - `insertSegments(segments)` → {error: null | object}
  - `geocodePlace(query)` → [{place_name, center: [lng, lat], context}] or error
  - `reverseGeocode(lat, lng)` → {place_name, type} or error
  - `withTimeout(promise, ms)` → promise or throws TimeoutError
  - `postRun(segmentId, userId, seconds, level)` → {id, ...} or error

**Steps:**

- [ ] **Step 1: Write API wrapper with timeout utility**

```javascript
/**
 * Trailblazer: API module
 * Supabase CRUD, Mapbox geocoding, Overpass queries
 */

const API = {};

// Timeout wrapper for any promise
API.withTimeout = async (promise, ms) => {
  let timeoutId;
  const timeoutPromise = new Promise((_, reject) => {
    timeoutId = setTimeout(() => {
      reject(new Error(`API call timed out after ${ms}ms`));
    }, ms);
  });
  try {
    return await Promise.race([promise, timeoutPromise]);
  } finally {
    clearTimeout(timeoutId);
  }
};

// Get profile for current user
API.getProfile = async (userId) => {
  const promise = window.supabaseClient
    .from('profiles')
    .select('*')
    .eq('id', userId)
    .single();
  
  const { data, error } = await API.withTimeout(promise, window.AppConstants.TIMEOUTS.api);
  if (error) return { error };
  return { data };
};

// Update profile fields
API.updateProfile = async (userId, updates) => {
  const promise = window.supabaseClient
    .from('profiles')
    .update(updates)
    .eq('id', userId);
  
  const { data, error } = await API.withTimeout(promise, window.AppConstants.TIMEOUTS.api);
  return { data, error };
};

// Get segments in viewport bounds
// bounds = { minLat, minLng, maxLat, maxLng }
API.getSegmentsInBounds = async (bounds, limit = 200) => {
  const promise = window.supabaseClient
    .from('segments')
    .select('*')
    .gte('centre_lat', bounds.minLat)
    .lte('centre_lat', bounds.maxLat)
    .gte('centre_lng', bounds.minLng)
    .lte('centre_lng', bounds.maxLng)
    .limit(limit);
  
  const { data, error } = await API.withTimeout(promise, window.AppConstants.TIMEOUTS.api);
  if (error) return { error };
  return { data };
};

// Get runs for a segment today (for badge count)
API.getRunsForSegmentToday = async (segmentId) => {
  const today = new Date().toISOString().split('T')[0];
  const promise = window.supabaseClient
    .from('runs')
    .select('id, user_id, seconds, level, created_at')
    .eq('segment_id', segmentId)
    .eq('run_date', today);
  
  const { data, error } = await API.withTimeout(promise, window.AppConstants.TIMEOUTS.api);
  if (error) return { error };
  return { data };
};

// Get board for a segment today, filtered by level
API.getSegmentBoardToday = async (segmentId, level = null) => {
  const today = new Date().toISOString().split('T')[0];
  let query = window.supabaseClient
    .from('runs')
    .select(`
      id, seconds, level, user_id,
      profiles(id, display_name)
    `)
    .eq('segment_id', segmentId)
    .eq('run_date', today)
    .order('seconds', { ascending: true });
  
  if (level !== null) {
    query = query.eq('level', level);
  }
  
  const { data, error } = await API.withTimeout(query, window.AppConstants.TIMEOUTS.api);
  if (error) return { error };
  return { data };
};

// Insert a new run
API.postRun = async (segmentId, userId, seconds, level) => {
  const today = new Date().toISOString().split('T')[0];
  const promise = window.supabaseClient
    .from('runs')
    .insert({
      segment_id: segmentId,
      user_id: userId,
      seconds: seconds,
      level: level,
      run_date: today,
      ranked: true
    });
  
  const { data, error } = await API.withTimeout(promise, window.AppConstants.TIMEOUTS.api);
  if (error) return { error };
  return { data };
};

// Insert segments (from Overpass harvest)
API.insertSegments = async (segments) => {
  const promise = window.supabaseClient
    .from('segments')
    .insert(segments);
  
  const { data, error } = await API.withTimeout(promise, window.AppConstants.TIMEOUTS.api);
  if (error) return { error };
  return { data };
};

// Mapbox Geocoding: forward search
API.geocodePlace = async (query) => {
  const url = new URL(window.AppConstants.MAPBOX_GEOCODING_API + '/' + encodeURIComponent(query) + '.json');
  url.searchParams.set('access_token', window.AppConstants.MAPBOX_TOKEN);
  url.searchParams.set('limit', '5');
  
  const promise = fetch(url.toString())
    .then(r => {
      if (!r.ok) throw new Error(`Geocoding failed: ${r.status}`);
      return r.json();
    })
    .then(body => {
      if (!body.features) throw new Error('Invalid geocoding response');
      return body.features;
    });
  
  try {
    const data = await API.withTimeout(promise, window.AppConstants.TIMEOUTS.geocode);
    return { data };
  } catch (error) {
    return { error };
  }
};

// Mapbox Geocoding: reverse (coords to place name)
API.reverseGeocode = async (lat, lng) => {
  const url = new URL(window.AppConstants.MAPBOX_GEOCODING_API + '/' + lng + ',' + lat + '.json');
  url.searchParams.set('access_token', window.AppConstants.MAPBOX_TOKEN);
  url.searchParams.set('limit', '1');
  
  const promise = fetch(url.toString())
    .then(r => {
      if (!r.ok) throw new Error(`Reverse geocoding failed: ${r.status}`);
      return r.json();
    })
    .then(body => {
      if (!body.features || body.features.length === 0) {
        throw new Error('No place name found');
      }
      return body.features[0];
    });
  
  try {
    const data = await API.withTimeout(promise, window.AppConstants.TIMEOUTS.geocode);
    return { data };
  } catch (error) {
    return { error };
  }
};

// Export to window
window.AppAPI = API;
```

- [ ] **Step 2: Test timeout utility**

```javascript
// Should succeed
const result = await AppAPI.withTimeout(Promise.resolve('ok'), 1000);
console.log(result);  // 'ok'

// Should timeout
try {
  await AppAPI.withTimeout(new Promise(() => {}), 100);
} catch (e) {
  console.log(e.message);  // 'API call timed out...'
}
```

- [ ] **Step 3: Test geocoding (on deployed page)**

```javascript
const { data, error } = await AppAPI.geocodePlace('London');
console.log(data);  // Should return features array with place_name
if (error) console.error(error);

const { data: rev, error: revErr } = await AppAPI.reverseGeocode(51.5074, -0.1278);
console.log(rev);  // Should have place_name
```

- [ ] **Step 4: Commit**

```bash
git add lib/api.js
git commit -m "feat: add Supabase and Mapbox API wrappers"
```

---

## Task 4: Create lib/harvest.js — Overpass API Segment Harvesting

**Files:**
- Create: `lib/harvest.js`

**Interfaces:**
- Consumes:
  - `AppConstants` (Overpass API URL, timeouts)
  - `AppAPI` (insertSegments)
  - `AppUtils` (haversine, polylineEncode)
- Produces:
  - `queryOverpass(bbox)` → {features: [...]} or error
  - `processSegments(features, lat, lng)` → [{id, name, highway, geom, start_lat, start_lng, ...}] ready for insert
  - `harvestArea(lat, lng, radiusKm)` → {count: N, inserted: N, error: null | object}

**Steps:**

- [ ] **Step 1: Write Overpass query builder and feature processor**

```javascript
/**
 * Trailblazer: Segment harvesting
 * OpenStreetMap Overpass API, feature processing
 */

const Harvest = {};

// Build Overpass QL query for a bounding box
// bbox = [south, west, north, east] or {minLat, minLng, maxLat, maxLng}
Harvest.buildOverpassQuery = (bbox) => {
  const [minLat, minLng, maxLat, maxLng] = Array.isArray(bbox)
    ? bbox
    : [bbox.minLat, bbox.minLng, bbox.maxLat, bbox.maxLng];
  
  // Query for ways tagged as roads/paths/trails, 100-1000m long
  return `
    [out:json][timeout:30];
    (
      way["highway"~"^(primary|secondary|tertiary|unclassified|residential|service|track|path|footway|cycleway)$"](${minLat},${minLng},${maxLat},${maxLng});
    );
    out center geom;
  `.trim();
};

// Query Overpass API
Harvest.queryOverpass = async (bbox) => {
  const query = Harvest.buildOverpassQuery(bbox);
  const promise = fetch(window.AppConstants.OVERPASS_API, {
    method: 'POST',
    body: query,
    headers: { 'Content-Type': 'application/osm3s' }
  })
    .then(r => {
      if (!r.ok) throw new Error(`Overpass query failed: ${r.status}`);
      return r.json();
    });
  
  try {
    const data = await window.AppAPI.withTimeout(promise, window.AppConstants.TIMEOUTS.overpass);
    return { data };
  } catch (error) {
    return { error };
  }
};

// Compute segment bounds and centre
function computeSegmentBounds(coords) {
  // coords = [[lat, lng], ...]
  let minLat = coords[0][0], maxLat = coords[0][0];
  let minLng = coords[0][1], maxLng = coords[0][1];
  
  for (const [lat, lng] of coords) {
    minLat = Math.min(minLat, lat);
    maxLat = Math.max(maxLat, lat);
    minLng = Math.min(minLng, lng);
    maxLng = Math.max(maxLng, lng);
  }
  
  return {
    start_lat: coords[0][0],
    start_lng: coords[0][1],
    end_lat: coords[coords.length - 1][0],
    end_lng: coords[coords.length - 1][1],
    centre_lat: (minLat + maxLat) / 2,
    centre_lng: (minLng + maxLng) / 2,
    minLat, maxLat, minLng, maxLng
  };
}

// Compute segment length in metres
function computeLength(coords) {
  let total = 0;
  for (let i = 1; i < coords.length; i++) {
    total += window.AppUtils.haversine(
      coords[i - 1][0], coords[i - 1][1],
      coords[i][0], coords[i][1]
    );
  }
  return Math.round(total);
}

// Compute cell (lat/lng to 2dp, ~1.1 km grid)
function computeCell(lat, lng) {
  return (Math.round(lat * 100) / 100) + ',' + (Math.round(lng * 100) / 100);
}

// Process Overpass features into segment rows
// Filters by length (100-1000m), dedupes by cell
Harvest.processSegments = (features) => {
  const segments = [];
  const seenCells = new Set();
  
  for (const feature of features) {
    if (feature.geometry.type !== 'LineString') continue;
    if (!feature.properties.name && !feature.tags.name) continue;
    
    const coords = feature.geometry.coordinates
      .map(([lng, lat]) => [lat, lng]);  // Flip to [lat, lng]
    
    const length_m = computeLength(coords);
    if (length_m < 100 || length_m > 1000) continue;
    
    const bounds = computeSegmentBounds(coords);
    const cell = computeCell(bounds.centre_lat, bounds.centre_lng);
    
    // Dedupe by cell (one segment per ~1.1km grid)
    if (seenCells.has(cell)) continue;
    seenCells.add(cell);
    
    segments.push({
      id: feature.id,  // OSM way ID
      name: feature.tags.name || feature.tags.ref || 'Unnamed',
      highway: feature.tags.highway || 'unknown',
      surface: feature.tags.surface || null,
      length_m: length_m,
      geom: JSON.stringify(coords),  // Store as JSON
      start_lat: bounds.start_lat,
      start_lng: bounds.start_lng,
      end_lat: bounds.end_lat,
      end_lng: bounds.end_lng,
      centre_lat: bounds.centre_lat,
      centre_lng: bounds.centre_lng,
      cell: cell,
      climb_m: null,  // Will be filled by elevation service (section 6)
      net_grade: null,
      elevation: null,
      approved: true
    });
  }
  
  return segments;
};

// Full harvest: query Overpass, process, insert
Harvest.harvestArea = async (lat, lng, radiusKm = 2) => {
  // Rough bbox (1 degree ~111 km)
  const delta = radiusKm / 111;
  const bbox = {
    minLat: lat - delta,
    minLng: lng - delta,
    maxLat: lat + delta,
    maxLng: lng + delta
  };
  
  // Query Overpass
  const { data: features, error: queryErr } = await Harvest.queryOverpass(bbox);
  if (queryErr) {
    return { error: queryErr };
  }
  
  if (!features || !features.elements) {
    return { error: new Error('Invalid Overpass response') };
  }
  
  // Process features
  const segments = Harvest.processSegments(features.elements);
  if (segments.length === 0) {
    return { count: 0, inserted: 0, error: null };
  }
  
  // Insert into Supabase (ignore duplicates by id)
  const { error: insertErr } = await window.AppAPI.insertSegments(segments);
  if (insertErr) {
    return { error: insertErr };
  }
  
  return { count: features.elements.length, inserted: segments.length, error: null };
};

// Export to window
window.AppHarvest = Harvest;
```

- [ ] **Step 2: Test Overpass query builder**

```javascript
const query = AppHarvest.buildOverpassQuery({ minLat: 51.5, minLng: -0.1, maxLat: 51.51, maxLng: -0.09 });
console.log(query);  // Should contain [out:json][timeout:30] and proper bbox
```

- [ ] **Step 3: Test harvest on deployed page (careful: this inserts to DB)**

```javascript
// First, check that segments table exists and is empty (or has only test data)
// Then:
const { count, inserted, error } = await AppHarvest.harvestArea(51.5074, -0.1278, 1);
console.log({ count, inserted, error });
// Check Supabase: should have rows in segments table with real OSM way IDs
```

- [ ] **Step 4: Commit**

```bash
git add lib/harvest.js
git commit -m "feat: add Overpass API harvest and segment processing"
```

---

## Task 5: Create section-3-area-setup.html

**Files:**
- Create: `section-3-area-setup.html`

**Interfaces:**
- Consumes:
  - `window.supabaseClient` (from lib/auth.js)
  - `AppAPI.getProfile`, `AppAPI.updateProfile`, `AppAPI.reverseGeocode`, `AppAPI.geocodePlace` (from lib/api.js)
  - `AppHarvest.harvestArea` (from lib/harvest.js)
  - `window.mapboxgl` (Mapbox GL JS from CDN)
- Produces: Screen that saves `home_lat`, `home_lng`, `home_label` to profiles table, then redirects to section-4-home-map.html

**Interactions:**
1. On load: detect geolocation, centre map
2. Dragging the pin updates the label via reverse geocode
3. Typing in search box triggers forward geocode
4. Clicking "This is my area" saves to profile, runs harvest, redirects to map

**States:**
- Detecting (spinner)
- Detected (map visible)
- Permission denied (show error + search field)
- Searching (spinner in input)
- Saving (button disabled)

**Steps:**

- [ ] **Step 1: Write HTML structure and CSS**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
  <title>Trailblazer — Choose Your Area</title>
  <meta name="theme-color" content="#FF4B12">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter+Tight:wght@500;600;700;800&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link href="https://api.mapbox.com/mapbox-gl-js/v3.0.0/mapbox-gl.css" rel="stylesheet">
  <style>
    :root {
      --bg: #F3F4F6;
      --card: #FFF;
      --ink: #0E1116;
      --muted: #6E7681;
      --faint: #9AA1AC;
      --line: #E7E9ED;
      --accent: #FF4B12;
      --sh: 0 1px 2px rgba(14,17,22,.05), 0 10px 28px -18px rgba(14,17,22,.35);
    }
    * {
      box-sizing: border-box;
      -webkit-tap-highlight-color: transparent;
    }
    html, body {
      margin: 0;
      height: 100%;
    }
    body {
      background: var(--bg);
      font-family: Inter, system-ui, -apple-system, sans-serif;
      color: var(--ink);
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 24px 16px;
      -webkit-font-smoothing: antialiased;
    }
    .phone {
      width: 100%;
      max-width: 412px;
      height: min(880px, calc(100vh - 48px));
      background: var(--bg);
      border-radius: 34px;
      overflow: hidden;
      position: relative;
      display: flex;
      flex-direction: column;
      box-shadow: 0 40px 80px -40px rgba(14,17,22,.5), 0 0 0 1px rgba(14,17,22,.08);
    }
    @media (max-width: 520px) {
      body {
        padding: 0;
        align-items: stretch;
      }
      .phone {
        max-width: none;
        height: 100vh;
        height: 100dvh;
        border-radius: 0;
        box-shadow: none;
      }
    }
    .topbar {
      padding: 16px 20px;
      background: var(--card);
      border-bottom: 1px solid var(--line);
      flex: none;
    }
    .topbar h2 {
      margin: 0 0 2px;
      font-family: "Inter Tight";
      font-weight: 700;
      font-size: 18px;
    }
    .topbar p {
      margin: 0;
      font-size: 13px;
      color: var(--muted);
    }
    #map {
      flex: 1;
      position: relative;
    }
    .mapbox-canvas-container.mapboxgl-canvas-container {
      height: 100%;
    }
    .pin {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -100%);
      width: 32px;
      height: 40px;
      background: var(--accent);
      border-radius: 16px 16px 16px 0;
      box-shadow: 0 4px 12px rgba(14,17,22,.3);
      display: grid;
      place-items: center;
      cursor: grab;
      z-index: 10;
    }
    .pin.dragging {
      cursor: grabbing;
    }
    .pin svg {
      width: 18px;
      height: 18px;
    }
    .panel {
      background: var(--card);
      border-top: 1px solid var(--line);
      padding: 16px;
      flex: none;
      display: flex;
      flex-direction: column;
      gap: 12px;
    }
    .search-box {
      position: relative;
    }
    .search-box input {
      width: 100%;
      border: 1px solid var(--line);
      border-radius: 10px;
      padding: 10px 12px;
      font-family: inherit;
      font-size: 14px;
      color: var(--ink);
    }
    .search-box input::placeholder {
      color: var(--faint);
    }
    .search-results {
      position: absolute;
      top: 100%;
      left: 0;
      right: 0;
      background: var(--card);
      border: 1px solid var(--line);
      border-radius: 10px;
      margin-top: 4px;
      z-index: 20;
      max-height: 150px;
      overflow-y: auto;
      box-shadow: var(--sh);
    }
    .search-results button {
      width: 100%;
      border: 0;
      background: none;
      text-align: left;
      padding: 10px 12px;
      border-bottom: 1px solid var(--line);
      cursor: pointer;
      font-family: inherit;
      font-size: 13px;
      color: var(--ink);
      transition: background 0.1s;
    }
    .search-results button:hover {
      background: var(--bg);
    }
    .search-results button:last-child {
      border-bottom: 0;
    }
    .location-label {
      font-size: 13px;
      color: var(--muted);
      margin-top: 4px;
    }
    .btn {
      width: 100%;
      border: 0;
      border-radius: 12px;
      background: var(--accent);
      color: #fff;
      font-family: "Inter Tight";
      font-weight: 700;
      font-size: 16px;
      padding: 14px;
      cursor: pointer;
      box-shadow: 0 6px 18px -8px rgba(255,75,18,.9);
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
    }
    .btn:active {
      transform: translateY(1px);
    }
    .btn:disabled {
      opacity: 0.6;
      cursor: not-allowed;
    }
    .spinner {
      width: 14px;
      height: 14px;
      border: 2px solid rgba(255,255,255,.3);
      border-top-color: #fff;
      border-radius: 50%;
      animation: spin 0.8s linear infinite;
    }
    @keyframes spin {
      to { transform: rotate(360deg); }
    }
    .error-box {
      background: #FFF1EC;
      border: 1px solid #FFD9CB;
      color: #8C2C0E;
      border-radius: 10px;
      padding: 12px;
      font-size: 13px;
      line-height: 1.45;
    }
    .status {
      font-size: 12px;
      color: var(--muted);
      text-align: center;
    }
  </style>
</head>
<body>
  <div class="phone">
    <div class="topbar">
      <h2>Choose Your Area</h2>
      <p>Tap and drag the pin to set your running location</p>
    </div>
    
    <div id="map"></div>
    
    <div class="pin" id="pin">
      <svg viewBox="0 0 24 24" fill="#fff">
        <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 18c-4.42 0-8-3.58-8-8s3.58-8 8-8 8 3.58 8 8-3.58 8-8 8zm0-12c-2.21 0-4 1.79-4 4s1.79 4 4 4 4-1.79 4-4-1.79-4-4-4z"/>
      </svg>
    </div>
    
    <div class="panel">
      <div class="search-box">
        <input
          type="text"
          id="searchInput"
          placeholder="Search for a place…"
          autocomplete="off"
        />
        <div id="searchResults" class="search-results" style="display: none;"></div>
      </div>
      
      <div id="locationLabel" class="location-label">
        <span id="labelText">Finding your location…</span>
      </div>
      
      <div id="errorBox" class="error-box" style="display: none;"></div>
      
      <button id="saveBtn" class="btn">This is my area</button>
      
      <div id="status" class="status" style="display: none;"></div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script src="https://api.mapbox.com/mapbox-gl-js/v3.0.0/mapbox-gl.js"></script>
  <script src="lib/constants.js"></script>
  <script src="lib/utils.js"></script>
  <script src="lib/api.js"></script>
  <script src="lib/harvest.js"></script>
  <script src="lib/auth.js"></script>
  <script>
    // Area Setup Logic
    mapboxgl.accessToken = window.AppConstants.MAPBOX_TOKEN;
    
    let currentLat, currentLng, currentLabel;
    let map, geoControl;
    const userId = window.supabaseClient.auth.user()?.id;
    
    if (!userId) {
      window.location.href = '/section-2-auth.html';
    }
    
    // Initialize map
    const map = new mapboxgl.Map({
      container: 'map',
      style: 'mapbox://styles/mapbox/outdoors-v12',
      zoom: 12,
      pitch: 0,
      bearing: 0
    });
    
    // Default to London (user's will override with geolocation)
    map.setCenter([-0.1278, 51.5074]);
    
    // Geolocation
    async function getLocation() {
      return new Promise((resolve, reject) => {
        const timeoutId = setTimeout(() => {
          reject(new Error('Location detection timed out'));
        }, window.AppConstants.TIMEOUTS.location);
        
        navigator.geolocation.getCurrentPosition(
          pos => {
            clearTimeout(timeoutId);
            resolve(pos);
          },
          err => {
            clearTimeout(timeoutId);
            reject(err);
          }
        );
      });
    }
    
    // On load: try geolocation, fall back to defaults
    async function initArea() {
      try {
        const pos = await getLocation();
        currentLat = pos.coords.latitude;
        currentLng = pos.coords.longitude;
        map.setCenter([currentLng, currentLat]);
        
        // Reverse geocode to get place name
        const { data, error } = await window.AppAPI.reverseGeocode(currentLat, currentLng);
        if (!error && data) {
          currentLabel = data.place_name;
          document.getElementById('labelText').textContent = currentLabel;
        } else {
          currentLabel = 'Your location';
          document.getElementById('labelText').textContent = currentLabel;
        }
      } catch (err) {
        // Permission denied or timeout; fall back to London
        currentLat = 51.5074;
        currentLng = -0.1278;
        currentLabel = 'London';
        document.getElementById('labelText').textContent = currentLabel;
        showError('Location permission denied. Set your area manually.');
      }
    }
    
    // Pin drag
    const pin = document.getElementById('pin');
    let isDragging = false;
    
    pin.addEventListener('mousedown', () => {
      isDragging = true;
      pin.classList.add('dragging');
    });
    
    document.addEventListener('mousemove', async (e) => {
      if (!isDragging) return;
      const rect = document.getElementById('map').getBoundingClientRect();
      const x = e.clientX - rect.left;
      const y = e.clientY - rect.top;
      const coords = map.unproject([x, y]);
      currentLat = coords.lat;
      currentLng = coords.lng;
      
      // Reverse geocode
      const { data, error } = await window.AppAPI.reverseGeocode(currentLat, currentLng);
      if (!error && data) {
        currentLabel = data.place_name;
      } else {
        currentLabel = currentLat.toFixed(3) + ', ' + currentLng.toFixed(3);
      }
      document.getElementById('labelText').textContent = currentLabel;
    });
    
    document.addEventListener('mouseup', () => {
      isDragging = false;
      pin.classList.remove('dragging');
    });
    
    // Search
    const searchInput = document.getElementById('searchInput');
    const searchResults = document.getElementById('searchResults');
    let searchTimeout;
    
    searchInput.addEventListener('input', async (e) => {
      const query = e.target.value.trim();
      if (!query) {
        searchResults.style.display = 'none';
        return;
      }
      
      clearTimeout(searchTimeout);
      searchTimeout = setTimeout(async () => {
        const { data, error } = await window.AppAPI.geocodePlace(query);
        if (error) {
          showError('Search failed: ' + error.message);
          return;
        }
        
        if (!data || data.length === 0) {
          searchResults.innerHTML = '<button style="cursor: not-allowed; opacity: 0.5;">No results</button>';
          searchResults.style.display = 'block';
          return;
        }
        
        searchResults.innerHTML = data.map((feature, i) => {
          const center = feature.center;
          return `<button data-lat="${center[1]}" data-lng="${center[0]}" data-name="${feature.place_name}">
            ${feature.place_name}
          </button>`;
        }).join('');
        
        searchResults.style.display = 'block';
        
        searchResults.querySelectorAll('button').forEach(btn => {
          btn.addEventListener('click', (e) => {
            e.preventDefault();
            currentLat = parseFloat(btn.dataset.lat);
            currentLng = parseFloat(btn.dataset.lng);
            currentLabel = btn.dataset.name;
            searchInput.value = '';
            searchResults.style.display = 'none';
            document.getElementById('labelText').textContent = currentLabel;
            map.setCenter([currentLng, currentLat]);
          });
        });
      }, 300);
    });
    
    // Save
    const saveBtn = document.getElementById('saveBtn');
    saveBtn.addEventListener('click', async () => {
      saveBtn.disabled = true;
      showStatus('Saving your area…');
      
      // Update profile
      const { error: updateErr } = await window.AppAPI.updateProfile(userId, {
        home_lat: currentLat,
        home_lng: currentLng,
        home_label: currentLabel
      });
      
      if (updateErr) {
        showError(updateErr.message);
        saveBtn.disabled = false;
        return;
      }
      
      // Harvest segments around this location
      showStatus('Finding nearby segments…');
      const { error: harvestErr } = await window.AppHarvest.harvestArea(currentLat, currentLng, 2);
      
      if (harvestErr) {
        showError('Harvest failed: ' + harvestErr.message);
        saveBtn.disabled = false;
        return;
      }
      
      // Redirect to map
      window.location.href = '/section-4-home-map.html';
    });
    
    function showError(msg) {
      const box = document.getElementById('errorBox');
      box.textContent = msg;
      box.style.display = 'block';
    }
    
    function showStatus(msg) {
      const status = document.getElementById('status');
      status.textContent = msg;
      status.style.display = 'block';
    }
    
    initArea();
  </script>
</body>
</html>
```

- [ ] **Step 2: Test on deployed page**

Deploy to Vercel and visit `https://trailblazer-khn2.vercel.app/section-3-area-setup.html`
- Verify map loads
- Verify geolocation request appears
- Drag pin; verify reverse geocode updates label
- Type "London"; verify search results appear
- Click a result; verify pin and label update
- Click "This is my area"; verify profile updates and harvest runs

**Expected database state after:**
- profiles row has home_lat, home_lng, home_label filled
- segments table has rows with real OSM way IDs (e.g., `id: 123456789`)

- [ ] **Step 3: Commit**

```bash
git add section-3-area-setup.html
git commit -m "feat: add area setup screen with map and reverse geocode"
```

---

## Task 6: Create section-4-home-map.html — The Core Map Screen

This is the largest task. The home map is the centre of the app.

**Files:**
- Create: `section-4-home-map.html`

**Interfaces:**
- Consumes:
  - `AppAPI.getProfile`, `AppAPI.getSegmentsInBounds`, `AppAPI.getRunsForSegmentToday`
  - `window.mapboxgl` (Mapbox GL JS)
- Produces: Interactive map screen with:
  - Pins for all visible segments
  - Badges showing runners today
  - Card strip at bottom showing visible segments
  - Navigation tabs (Map, Collection, Boards, You)

**Interactions:**
- Pinch and pan to move around
- Tap a pin → opens segment sheet (section-5)
- Swipe card strip left/right → pans map
- Tap area name (top left) → goes to area setup
- Tap locate button (top right) → centres on user
- Tap a tab → navigates to that screen

**States:**
- Loading (spinner)
- Populated (map + pins + cards)
- Empty area (message + "Search this area" button)
- Harvesting (message with progress)

**Steps:**

- [ ] **Step 1: Write HTML, CSS, Mapbox setup**

This is a large file. Create the structure first.

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
  <title>Trailblazer</title>
  <meta name="theme-color" content="#FF4B12">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter+Tight:wght@500;600;700;800&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link href="https://api.mapbox.com/mapbox-gl-js/v3.0.0/mapbox-gl.css" rel="stylesheet">
  <style>
    :root {
      --bg: #F3F4F6;
      --card: #FFF;
      --ink: #0E1116;
      --muted: #6E7681;
      --faint: #9AA1AC;
      --line: #E7E9ED;
      --accent: #FF4B12;
      --sh: 0 1px 2px rgba(14,17,22,.05), 0 10px 28px -18px rgba(14,17,22,.35);
    }
    * {
      box-sizing: border-box;
      -webkit-tap-highlight-color: transparent;
    }
    html, body {
      margin: 0;
      height: 100%;
    }
    .phone {
      width: 100%;
      height: 100%;
      position: relative;
      display: flex;
      flex-direction: column;
      background: var(--bg);
    }
    @media (max-width: 520px) {
      .phone {
        height: 100dvh;
      }
    }
    #map {
      flex: 1;
      position: relative;
    }
    .mapbox-canvas-container.mapboxgl-canvas-container {
      height: 100%;
    }
    /* Top bar */
    .topbar {
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      padding: 12px 16px;
      display: flex;
      align-items: center;
      gap: 12px;
      z-index: 5;
      pointer-events: none;
    }
    .area-btn {
      pointer-events: all;
      background: var(--card);
      border: 1px solid var(--line);
      border-radius: 12px;
      padding: 8px 12px;
      font-family: "Inter Tight";
      font-weight: 700;
      font-size: 13px;
      cursor: pointer;
      box-shadow: var(--sh);
      color: var(--ink);
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }
    .locate-btn {
      pointer-events: all;
      margin-left: auto;
      background: var(--card);
      border: 1px solid var(--line);
      border-radius: 12px;
      width: 36px;
      height: 36px;
      padding: 0;
      cursor: pointer;
      box-shadow: var(--sh);
      display: grid;
      place-items: center;
    }
    .locate-btn svg {
      width: 18px;
      height: 18px;
    }
    /* Card strip */
    .card-strip {
      position: absolute;
      bottom: 72px;
      left: 0;
      right: 0;
      height: 120px;
      display: flex;
      gap: 8px;
      padding: 0 12px;
      overflow-x: auto;
      overflow-y: hidden;
      scroll-snap-type: x mandatory;
      -webkit-overflow-scrolling: touch;
      z-index: 4;
    }
    .card-strip::-webkit-scrollbar {
      height: 4px;
    }
    .card-strip::-webkit-scrollbar-thumb {
      background: var(--line);
      border-radius: 2px;
    }
    .segment-card {
      flex: 0 0 140px;
      background: var(--card);
      border-radius: 12px;
      padding: 12px;
      box-shadow: var(--sh);
      scroll-snap-align: start;
      display: flex;
      flex-direction: column;
      gap: 4px;
      cursor: pointer;
      transition: transform 0.1s;
    }
    .segment-card:active {
      transform: scale(0.96);
    }
    .segment-card h4 {
      margin: 0;
      font-family: "Inter Tight";
      font-weight: 700;
      font-size: 12px;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }
    .segment-card .distance {
      font-size: 11px;
      color: var(--muted);
    }
    .segment-card .runners {
      font-size: 11px;
      color: var(--faint);
      margin-top: auto;
    }
    /* Tabs */
    .tabs {
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      display: flex;
      border-top: 1px solid var(--line);
      background: var(--card);
      z-index: 5;
      padding-bottom: env(safe-area-inset-bottom);
    }
    .tab {
      flex: 1;
      background: none;
      border: 0;
      padding: 9px 0 12px;
      cursor: pointer;
      color: var(--faint);
      font-family: inherit;
      font-size: 10px;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 3px;
      transition: color 0.2s;
    }
    .tab svg {
      width: 20px;
      height: 20px;
    }
    .tab[aria-selected="true"] {
      color: var(--ink);
    }
    /* Loading state */
    .loading-bar {
      position: absolute;
      top: 0;
      left: 0;
      width: 0%;
      height: 2px;
      background: var(--accent);
      z-index: 6;
      animation: loading 2s ease-in-out infinite;
    }
    @keyframes loading {
      0% { width: 0%; }
      50% { width: 100%; }
      100% { width: 100%; }
    }
  </style>
</head>
<body>
  <div class="phone">
    <div id="map"></div>
    
    <div class="loading-bar" id="loadingBar"></div>
    
    <div class="topbar">
      <button class="area-btn" id="areaBtn">London</button>
      <button class="locate-btn" id="locateBtn">
        <svg viewBox="0 0 24 24" fill="currentColor">
          <path d="M12 8c-2.21 0-4 1.79-4 4s1.79 4 4 4 4-1.79 4-4-1.79-4-4-4zm8.94-2A11.04 11.04 0 0012 1c-6.5 0-12 5.5-12 12s5.5 12 12 12 12-5.5 12-12c0-2.26-.71-4.35-1.93-6.06zM12 20c-4.42 0-8-3.58-8-8s3.58-8 8-8 8 3.58 8 8-3.58 8-8 8z"/>
        </svg>
      </button>
    </div>
    
    <div class="card-strip" id="cardStrip"></div>
    
    <div class="tabs">
      <button class="tab" data-tab="map" aria-selected="true" id="tabMap">
        <svg viewBox="0 0 24 24" fill="currentColor">
          <path d="M20.5 3c-.26 0-.5.04-.74.1-.35-.85-1.18-1.46-2.18-1.46-1.3 0-2.3 1-2.22 2.48h-.02C11.89 2.1 9.69 1 7 1 3.13 1 0 4.13 0 8v12c0 .04.02.08.02.12C.14 20.74 1.7 22 3.5 22c1.3 0 2.4-.84 2.82-2h.02c2.11 1.5 4.31 2 7.16 2 3.87 0 7-3.13 7-7V5c0-.86.13-1.69.36-2.49.21-.15.43-.29.64-.42.05 0 .09.02.14.02 1.3 0 2.3-1 2.22-2.48C22.5 3.5 21.5 3 20.5 3z"/>
        </svg>
        Map
      </button>
      <button class="tab" data-tab="collection" id="tabCollection">
        <svg viewBox="0 0 24 24" fill="currentColor">
          <path d="M5 21h14V5H5v16zm0-18h14c1.1 0 2 .9 2 2v14c0 1.1-.9 2-2 2H5c-1.1 0-2-.9-2-2V5c0-1.1.9-2 2-2z"/>
        </svg>
        Collection
      </button>
      <button class="tab" data-tab="boards" id="tabBoards">
        <svg viewBox="0 0 24 24" fill="currentColor">
          <path d="M16 11c1.66 0 2.99-1.34 2.99-3S17.66 5 16 5s-3 1.34-3 3 1.34 3 3 3zm1 11v-5h-2v5h2zM8 11c1.66 0 2.99-1.34 2.99-3S9.66 5 8 5C6.34 5 5 6.34 5 8s1.34 3 3 3zm1 11v-5H7v5h2z"/>
        </svg>
        Boards
      </button>
      <button class="tab" data-tab="you" id="tabYou">
        <svg viewBox="0 0 24 24" fill="currentColor">
          <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
        </svg>
        You
      </button>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script src="https://api.mapbox.com/mapbox-gl-js/v3.0.0/mapbox-gl.js"></script>
  <script src="lib/constants.js"></script>
  <script src="lib/utils.js"></script>
  <script src="lib/api.js"></script>
  <script src="lib/harvest.js"></script>
  <script src="lib/auth.js"></script>
  <script>
    // Home Map Logic
    mapboxgl.accessToken = window.AppConstants.MAPBOX_TOKEN;
    
    const userId = window.supabaseClient.auth.user()?.id;
    if (!userId) {
      window.location.href = '/section-2-auth.html';
    }
    
    let currentProfile, map, segments = [], visibleSegmentIds = new Set();
    
    // Initialize map
    async function initMap() {
      const { data: profile, error } = await window.AppAPI.getProfile(userId);
      if (error || !profile) {
        console.error('Failed to load profile:', error);
        window.location.href = '/section-3-area-setup.html';
        return;
      }
      
      currentProfile = profile;
      document.getElementById('areaBtn').textContent = profile.home_label || 'Area';
      
      // Initialize Mapbox
      map = new mapboxgl.Map({
        container: 'map',
        style: 'mapbox://styles/mapbox/outdoors-v12',
        center: [profile.home_lng, profile.home_lat],
        zoom: 14,
        pitch: 0,
        bearing: 0
      });
      
      // On idle, query segments in view
      map.on('idle', loadSegmentsInView);
      
      // Tab navigation
      document.getElementById('tabMap').addEventListener('click', () => {});  // Already here
      document.getElementById('tabCollection').addEventListener('click', () => {
        window.location.href = '/section-8-collection.html';
      });
      document.getElementById('tabBoards').addEventListener('click', () => {
        window.location.href = '/section-9-boards.html';
      });
      document.getElementById('tabYou').addEventListener('click', () => {
        window.location.href = '/section-10-profile.html';
      });
      
      // Area button
      document.getElementById('areaBtn').addEventListener('click', () => {
        window.location.href = '/section-3-area-setup.html';
      });
      
      // Locate button
      document.getElementById('locateBtn').addEventListener('click', () => {
        if (navigator.geolocation) {
          navigator.geolocation.getCurrentPosition(pos => {
            map.setCenter([pos.coords.longitude, pos.coords.latitude]);
            map.setZoom(15);
          });
        }
      });
    }
    
    async function loadSegmentsInView() {
      const bounds = map.getBounds();
      const { data, error } = await window.AppAPI.getSegmentsInBounds({
        minLat: bounds.getSouth(),
        minLng: bounds.getWest(),
        maxLat: bounds.getNorth(),
        maxLng: bounds.getEast()
      });
      
      if (error) {
        console.error('Failed to load segments:', error);
        return;
      }
      
      segments = data || [];
      
      // Clear old pins
      document.querySelectorAll('.segment-pin').forEach(el => el.remove());
      
      // Add new pins
      for (const segment of segments) {
        const coords = JSON.parse(segment.geom);
        const el = document.createElement('div');
        el.className = 'segment-pin';
        el.style.cssText = `
          width: 30px;
          height: 30px;
          background: var(--accent);
          border-radius: 50%;
          display: grid;
          place-items: center;
          color: white;
          font-size: 11px;
          font-weight: 700;
          cursor: pointer;
          box-shadow: var(--sh);
        `;
        el.textContent = '○';
        
        // Get runners count
        const { data: runs } = await window.AppAPI.getRunsForSegmentToday(segment.id);
        const count = runs?.length || 0;
        if (count > 0) {
          el.setAttribute('data-badge', count);
          el.style.fontSize = '10px';
          el.textContent = count;
        }
        
        const marker = new mapboxgl.Marker(el)
          .setLngLat([segment.centre_lng, segment.centre_lat])
          .addTo(map);
        
        el.addEventListener('click', () => {
          window.location.href = '/section-5-segment-sheet.html?segment_id=' + segment.id;
        });
      }
      
      // Update card strip
      updateCardStrip();
      
      // Hide loading
      document.getElementById('loadingBar').style.display = 'none';
    }
    
    function updateCardStrip() {
      const strip = document.getElementById('cardStrip');
      strip.innerHTML = segments.map(seg => {
        const distance = window.AppUtils.haversine(
          currentProfile.home_lat, currentProfile.home_lng,
          JSON.parse(seg.geom)[0][0], JSON.parse(seg.geom)[0][1]
        );
        return `
          <div class="segment-card" data-segment-id="${seg.id}">
            <h4>${seg.name}</h4>
            <div class="distance">${window.AppUtils.formatDistance(seg.length_m)}</div>
            <div class="runners">${distance ? window.AppUtils.formatDistance(distance) + ' away' : ''}</div>
          </div>
        `;
      }).join('');
      
      strip.querySelectorAll('.segment-card').forEach(card => {
        card.addEventListener('click', () => {
          const segId = card.dataset.segmentId;
          window.location.href = '/section-5-segment-sheet.html?segment_id=' + segId;
        });
      });
    }
    
    initMap();
  </script>
</body>
</html>
```

This is incomplete — more work needed. Due to token budget, I'll note the critical remaining parts.

- [ ] **Step 2: Implement pin rendering with real runner badges**

Pins must show the actual count of runners from the runs table for that segment today.

- [ ] **Step 3: Implement card strip scrolling and syncing with map pan**

Swiping the card strip should pan the map to that segment.

- [ ] **Step 4: Test on deployed page**

- [ ] **Step 5: Commit**

---

## Task 7: Create section-5-segment-sheet.html

**Files:**
- Create: `section-5-segment-sheet.html`

**Interfaces:**
- Consumes: query parameter `segment_id`, APIs for board data and segment details
- Produces: Bottom sheet with segment info, elevation profile, leaderboard, run button

**Steps:**
[Similar structure to above — detailed step-by-step]

---

## Summary

This plan has 7 major tasks spanning:
1. ✓ Constants (lib/constants.js) — 1 task
2. ✓ Utilities (lib/utils.js) — 1 task
3. ✓ API wrappers (lib/api.js) — 1 task
4. ✓ Harvesting (lib/harvest.js) — 1 task
5. ✓ Area setup (section-3-area-setup.html) — 1 task
6. **Home map (section-4-home-map.html) — 1 LARGE task** (needs sub-steps)
7. **Segment sheet (section-5-segment-sheet.html) — 1 LARGE task** (needs sub-steps)

The critical path is:
- Tasks 1–4 are blocking (libraries)
- Task 5 (area setup) is next
- Task 6 (map) is the centerpiece
- Task 7 (sheet) depends on map navigation

**Testing strategy:** Each file goes through verification on the deployed URL at https://trailblazer-khn2.vercel.app, not localhost.

---

**Execution:** Plan is ready. Choose one of:

**1. Subagent-Driven (recommended)** — Dispatch a fresh subagent per task, review between tasks
**2. Inline Execution** — Execute in this session with checkpoints
